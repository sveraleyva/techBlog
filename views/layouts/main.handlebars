<head>
  <meta charset="UTF-8">
  <title>The Tech Blog</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="flex flex-col justify-around bg-[#A3C4BC]">
    <header>
      {{!-- Code copied (but changed by me) from Flobite: https://flowbite.com/docs/components/navbar/ --}}
      <nav class="bg-[#BFD7B5] fixed w-full z-20 top-0 left-0 border-b border-gray-200">
        <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
          <a href="https://flowbite.com/" class="flex items-center">
              <img src="https://flowbite.com/docs/images/logo.svg" class="h-8 mr-3" alt="Flowbite Logo">
              <span class="self-center text-2xl font-semibold whitespace-nowrap">The Tech Blog</span>
          </a>
          <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-sticky">
            <ul class="flex flex-col p-4 md:p-0 mt-4 font-medium border border-gray-100 rounded-lg md:flex-row md:space-x-8 md:mt-0 md:border-0">
              <li>
                <a href="#" class="block py-2 pl-3 pr-4 text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-blue-700 md:p-0">Dashboard</a>
              </li>
              {{#if loggedIn}}
              <li>
                <button class="block py-2 pl-3 pr-4 text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-blue-700 md:p-0" id="logout">Log Out</button>
              </li>
              {{else}}
              <li>
                <a class="block py-2 pl-3 pr-4 text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-blue-700 md:p-0" id="login" href="/login">Log In</a>
              </li>
              {{/if}}
            </ul>
          </div>
        </div>
      </nav>
    {{!-- End of flowbite code --}}
    </header>
    
    <main class="pt-20">
      {{{ body }}}
    </main>
    
    <footer class="bg-[#BFD7B5] mt-4 border-t border-gray-200 fixed bottom-0 w-full">
        <div class="mx-auto w-full max-w-screen-xl p-4 py-2">
            <div class="md:flex md:justify-between">
              <div class="mb-6 md:mb-0">
                  <a href="https://flowbite.com/" class="flex items-center">
                      <span class="self-center text-sm font-semibold whitespace-nowrap">Made by Samantha Vera Leyva</span>
                  </a>
              </div>
          </div>
        </div>
    </footer>

  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>

